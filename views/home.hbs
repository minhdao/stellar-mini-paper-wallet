
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title></title>
    <script type="text/javascript" src="qrcode/build/qrcode.min.js"></script>
    <script src="bootstrap/dist/js/bootstrap.js"></script>
    <link href="bootstrap/dist/css/bootstrap.css" type="text/css" rel="stylesheet" />
    <link href="css/style.css" type="text/css" rel="stylesheet" />
</head>
{{> header}}
<body>

    <div id="container-freeStyle">
      <div class="alert alert-warning" role="alert">
        <strong>Attention:
        </strong>After generate the paper wallet, you can verify the account on <a href="https://www.stellar.org/account-viewer/" target="_blank">Stellar Account Viewer</a>.
        <br/>However, the account doesn't exist on the network. To create the account, send it at least 1 lumen (XLM) to fulfill the minimum balance.
      </div>
      <div class="container-list-item">
        <div class="title-item">
          <span>1. First style</span>
          <div class="btn btn-success">Generate</div>
          <div class="btn btn-primary">Print</div>
          <div class="btn btn-danger">How to fold</div>
        </div>
        <div class="img-item">
            <img src="/img/style1.png"/>
            <div id="container-public">
              <canvas id="publicCanvas"></canvas>
              <p id="publicText">{{publicKey}}</p>
            </div>
            <div id="container-private">
              <canvas id="privateCanvas"></canvas>
              <div id="privateText">{{privateKey}}</div>
            </div>

        </div>

      </div>
    </div>


      <!-- <div class="col-lg-12" style="text-align:center">


            <div class=" lineFolding testPublic">
                <h3>Public Key</h3>
                <canvas id="publicCanvas"></canvas>
                <p id="publicText">{{publicKey}}</p>
            </div>
            <div class=" lineFolding testInfo">
                <h3>Information</h3>
                <canvas id="publicCanvas"></canvas>
            </div>
            <div class=" lineFolding testPrivate">
                <h3>Private Key</h3>
                <canvas id="privateCanvas"></canvas>
                <p id="privateText">{{privateKey}}</p>
            </div>
        </div>
    </div>
    <div id="footer"></div>
    <canvas id="canvas" width="200" height="100"></canvas>-->
</body>
{{> footer}}
</html>
<script>
    QRCode.toCanvas(document.getElementById('publicCanvas'), '{{publicKey}}', function (error) {
        if (error) console.error(error)
        console.log('success!');
    })
    QRCode.toCanvas(document.getElementById('privateCanvas'), '{{privateKey}}', function (error) {
        if (error) console.error(error)
        console.log('success!');
    })
</script>
